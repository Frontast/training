let count = 42
let n = -3
let m = -10
let range = Math.abs(m - n)
let array = []
for (let i = 1; i <= count; ++i) {
  array.push(Math.round(Math.random() * range) + Math.min(n, m))
}
console.log(array)
